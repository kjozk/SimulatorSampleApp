<UserControl x:Class="SimulatorSampleApp.UI.Views.PlaneConditionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SimulatorSampleApp.UI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <TextBlock Text="Origin X" />
        <TextBox Text="{Binding OriginX}" />

        <TextBlock Text="Origin Y" />
        <TextBox Text="{Binding OriginY}" />

        <TextBlock Text="Width" />
        <TextBox Text="{Binding Width}" />

        <TextBlock Text="Depth" />
        <TextBox Text="{Binding Depth}" />

        <TextBlock Text="Point Count X" />
        <TextBox Text="{Binding PointCountX}" />

        <TextBlock Text="Point Count Y" />
        <TextBox Text="{Binding PointCountY}" />
    </StackPanel>
</UserControl>
